import 'package:flutter/material.dart';
class HardModeScreen extends StatelessWidget {
  const HardModeScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Hard Mode')));
}
